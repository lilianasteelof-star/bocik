# EWH-WatchDog — zależności (Python 3.10+)
# Bot i HTTP
aiogram==3.12.0
aiohttp>=3.9.0,<4

# Baza danych (SQLite lub PostgreSQL)
aiosqlite==0.20.0
asyncpg>=0.30.0,<0.32

# Planowanie zadań
apscheduler==3.10.4

# Konfiguracja i walidacja
pydantic==2.8.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Opcjonalnie — SFS (średnia wyświetleń/post)

# telethon>=1.34.0
